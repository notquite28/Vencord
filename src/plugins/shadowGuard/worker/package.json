{
  "name": "shadowguard-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for ShadowGuard key exchange",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:preview": "wrangler deploy --env preview"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.0.0"
  }
}
